import { MultiPageContext } from "@/lib/intel/types"

export const mockContext: MultiPageContext = {
  sessionId: "debug-session",
  lang: "en",
  geoCountry: "US",
  season: "winter",
  device: "desktop"
}
